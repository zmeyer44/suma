<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the signed Suma.app (see docs/release-macos.md).

  - allow-jit / allow-unsigned-executable-memory: V8 needs both under the
    hardened runtime.
  - disable-library-validation: required to load the Widevine CDM (signed by
    Google, not by our Developer ID) in castLabs ECS builds.
  - audio-input / camera: getUserMedia for sites; the matching usage strings
    are injected via extendInfo in electron-builder.prod.yml.
  - keychain-access-groups: Touch ID passkeys live here; must equal the
    runtime group <TEAM_ID>.<BUNDLE_ID>.webauthn (docs/auth-flows.md).
    Hardcoded to team PHSRT54C87 (2026-08-13): electron-builder hands this
    plist to codesign verbatim, so $(AppIdentifierPrefix) never expands.
    Verify after signing (grep webauthn in the entitlements codesign prints;
    the exact command is in docs/release-macos.md section 4). Beware: a
    double hyphen is ILLEGAL inside an XML comment, and Apple's entitlement
    parser (AMFIUnserializeXML) hard-fails the whole signing step on it, so
    never paste flag-bearing commands into this comment.
-->
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.device.camera</key>
    <true/>
    <key>keychain-access-groups</key>
    <array>
      <string>PHSRT54C87.com.sumabrowser.app.webauthn</string>
    </array>
  </dict>
</plist>
