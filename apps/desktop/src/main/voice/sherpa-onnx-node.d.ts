/**
 * sherpa-onnx-node ships no TypeScript declarations. The module is loaded
 * dynamically and immediately narrowed to the `SherpaModule` slice declared
 * in wake-word.ts, so an opaque module type is all that is needed here.
 */
declare module "sherpa-onnx-node";
