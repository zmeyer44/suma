{
  "name": "@suma/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared Suma constants and the seed supported-origin corpus.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src test --max-warnings 0"
  },
  "dependencies": {
    "@suma/protocol": "workspace:*"
  },
  "devDependencies": {
    "@suma/eslint-config": "workspace:*",
    "@suma/typescript-config": "workspace:*",
    "eslint": "^9.39.1",
    "typescript": "5.9.2",
    "vitest": "^3.2.0"
  }
}
