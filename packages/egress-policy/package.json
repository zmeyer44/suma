{
  "name": "@suma/egress-policy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Identity egress routing decisions (PRD §8.4): per-space policy, per-site bypass, media/local/VPN rules, QUIC handling, fail-closed behavior.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src test --max-warnings 0"
  },
  "dependencies": {
    "@suma/config": "workspace:*",
    "@suma/protocol": "workspace:*"
  },
  "devDependencies": {
    "@suma/eslint-config": "workspace:*",
    "@suma/typescript-config": "workspace:*",
    "eslint": "^9.39.1",
    "typescript": "5.9.2",
    "vitest": "^3.2.0"
  }
}
