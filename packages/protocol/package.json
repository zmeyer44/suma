{
  "name": "@suma/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared Suma protocol: HLC, cookie identity, key hierarchy, sealed records, sync wire messages. Runs in Node, Cloudflare Workers, and Electron (WebCrypto only, no Node built-ins).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "check-types": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint src test --max-warnings 0"
  },
  "dependencies": {
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@suma/eslint-config": "workspace:*",
    "@suma/typescript-config": "workspace:*",
    "eslint": "^9.39.1",
    "typescript": "5.9.2",
    "vitest": "^3.2.0"
  }
}
